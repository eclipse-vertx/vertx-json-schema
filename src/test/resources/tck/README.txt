The content of this directory should be synchronized from time to time from the official repo:

https://github.com/json-schema-org/JSON-Schema-Test-Suite

include the "test/*" we need to verify here, plus the "remotes" directory.
